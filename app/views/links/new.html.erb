<h1>Create a new shortened url</h1>
<p>Fill the form below</p>

<%= form_for @link do |f| %>
  <%= f.label :original_url %>
  <%= f.text_field :original_url, placeholder: "Enter an url" %>

  <%= f.button "Create shortened url" %>
<% end %>